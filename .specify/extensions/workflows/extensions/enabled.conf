# Specify Extension Configuration
# Enabled workflows are loaded at runtime
# Comment out a line to disable that workflow extension

# Extension Workflows
baseline
bugfix
enhance
modify
refactor
hotfix
deprecate
cleanup

# To add custom extensions, add the workflow name here
# and place files in .specify/extensions/workflows/{name}/
